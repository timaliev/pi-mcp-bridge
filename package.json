{
  "name": "pi-mcp-bridge",
  "version": "1.0.0",
  "description": "MCP (Model Context Protocol) bridge for pi — connects MCP servers and registers their tools",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "typescript": "^7.0.2"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
